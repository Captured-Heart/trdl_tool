import 'package:trdl_tool/core/all_imports.dart';

class ProQuiz extends StatelessWidget {
  const ProQuiz({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return const ProQuizScreen();
  }
}
