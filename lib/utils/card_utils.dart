import '/all_imports.dart';

//All constants all over app should use THESE constants!
class CardUtils {
  static const double kCardElevation = 8.0;
  static const EdgeInsets kCardPadding = EdgeInsets.all(12.0);
}